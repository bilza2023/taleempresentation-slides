export default function setHandlesForEachItem(selectedItem: any): (AdderHandle | DraggerHandle)[];
import AdderHandle from '../handleObject/AdderHandle';
import DraggerHandle from '../handleObject/DraggerHandle';
